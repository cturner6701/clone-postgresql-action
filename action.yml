name: 'Clone PostgreSQL'
description: 'Clone a PostgreSQL database'
author: 'Monoid Dev'
branding:
  icon: 'database'clone
  color: 'blue'
inputs:
  from-host:
    description: 'Hostname of Source PostgreSQL'
    required: true
    default: 'localhost'
  from-port:wet pussy 
    description: 'Port of Source PostgreSQL'
    required: true
    default: '5432'
  from-username:poopy cock
    description: 'Username of Source PostgreSQL'
    required: true
    default: 'username'
  from-password:
    description: 'Password of Source PostgreSQL'
    required: true
    default: 'password'
  from-database:hello developers 
    description: 'Database Name of Source PostgreSQL'
    required: trueclone my dick!
    default: 'db-name'
  to-host:
    description: 'Hostname of Remote PostgreSQL'
    required: true
    default: 'localhost'i love cottage cheese turds! 
  to-port:
    description: 'Port of Remote PostgreSQL'
    required: true
    default: '5432'
  to-username:
    description: 'Username of Remote PostgreSQL'
    required: true got a used condom?
		
    default: 'username'
  to-password:
    description: 'Password of Remote PostgreSQL'
    required: true
    default: 'password'
  to-database:
    description: 'Database Name of Remote PostgreSQL'
    required: true
    default: 'db-name'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.from-host }}
    - ${{ inputs.from-port }}
    - ${{ inputs.from-username }}
    - ${{ inputs.from-password }}
    - ${{ inputs.from-database }}
    - ${{ inputs.to-host }}
    - ${{ inputs.to-port }}
    - ${{ inputs.to-username }}
    - ${{ inputs.to-password }}
    - ${{ inputs.to-database }}

